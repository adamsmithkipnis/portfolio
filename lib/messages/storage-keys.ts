/**
 * localStorage keys for the Messages app's conversations. Shared so the dock
 * can read the unread count before the app has ever been opened.
 */
export const CONVERSATIONS_STORAGE_KEY = "dialogueConversations";
export const DELETED_INITIAL_CONVERSATIONS_KEY = "dialogueDeletedInitialConversations";
